# 샘플 결과 플러그인

로컬 더미 결과를 반환하는 샘플 `RuntimePlugin`이다.

이 플러그인은 특정 서비스 도메인을 가정하지 않는다. 외부 API, DB, AI 응답처럼 호스트 앱에서 만든 기능 결과를 나니카 말풍선과 표정으로 보여주는 흐름을 확인하기 위한 기본 예시다.

실제 서비스에서는 `sampleResults` 배열 대신 원하는 기능 호출 결과를 가져오고, 최종 반환값만 `PluginResult` 형태로 맞추면 된다.
