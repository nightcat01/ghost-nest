# AGENTS.md

## 역할

이 파일은 GhostNest 프로젝트 전용 하네스 어댑터다.

- 공용 작업 규칙의 기준점은 `E:\personal project\agent_harness`이다.
- 공용 규칙을 이 파일에 복사해서 중복 관리하지 않는다.
- 공용 규칙을 바꿔야 하면 GhostNest 안에서 임시로 덮어쓰지 말고, 공유 하네스 쪽 변경 필요성을 먼저 분리해 판단한다.
- 이 파일에는 GhostNest에서만 필요한 오버라이드, 경계, 검증 기준만 둔다.

## 적용 순서

1. 현재 대화의 시스템/개발자/사용자 지시를 우선 적용한다.
2. 공용 하네스 `E:\personal project\agent_harness\AGENTS.md`를 기본 규칙으로 적용한다.
3. 공용 A2A 흐름은 `E:\personal project\agent_harness\docs\ai\README.md` 색인을 통해 필요한 문서만 적용한다.
4. GhostNest 도메인 판단이 필요하면 `docs/ai/project/HARNESS_INDEX.md`를 먼저 확인한다.
5. 공용 규칙과 GhostNest 전용 규칙이 충돌하면 충돌 내용을 보고하고, GhostNest 전용 규칙을 임시 우선 적용한다.

## GhostNest UI/UX Verification Override

- GhostNest는 UI 중심 프로젝트이므로 devtools, runtime, mapping editor, character editor 변경은 실제 화면 검증 없이는 완료로 보고하지 않는다.
- `npm run check`, `npm run build`, API 200 확인은 UI/UX 검증을 대체하지 않는다.
- UI 변경 후에는 브라우저/스크린샷/DOM 측정/사용자 최신 캡처 중 하나 이상으로 레이아웃, 스크롤, 겹침, 입력 크기, 버튼 위치를 확인한다.
- 브라우저 자동 검증이 실패하면 "정적 검증 완료, UX 검증 미완료"로 보고하고 완료 표현을 피한다.
- 같은 UI 문제가 반복되면 추가 패치 전 Rework Stop Rule을 적용해 원인, 불변조건, 검증 방법을 먼저 적는다.
- 같은 문제가 2회 이상 반복되면 새 패치 전에 사용자 최신 증상과 이전 가설의 충돌 지점을 먼저 정리한다.
- 같은 문제가 3회 이상 반복되면 사용자와 새 원인 가설을 맞추기 전에는 파일을 수정하지 않는다.
- 에디터와 런타임 결과가 다르게 보이는 UI 문제는 저장 데이터, 부모 박스, 레이어 박스, 이미지 박스, computed style, 실제 rect를 비교한 뒤 수정한다.

## GhostNest Boundary Override

- 코어와 런타임은 최소 실행 계약과 렌더링 계약만 책임진다.
- devtools는 플러그인 또는 데모 기능을 만들고 검수하기 위한 도구이며, 코어 동작의 필수 전제가 되면 안 된다.
- 플러그인은 기능 단위로 분리하고, devtools가 필요하면 해당 플러그인 내부에 둘 수 있다.
- 데모 데이터와 샘플 캐릭터는 실사용 기본값처럼 보이지 않도록 경계를 명확히 한다.
- 저장소가 파일인지 DB인지는 데이터 어댑터 경계에서 갈라야 하며, 런타임과 매핑 UI가 저장 방식 세부사항을 직접 알아서는 안 된다.

## GhostNest UI Flow Override

- 캐릭터 설정, 매핑 설정, 메뉴 설정처럼 단계가 있는 화면은 시작 지점을 왼쪽 또는 상단에 두고, 선택 결과와 저장/완료 지점이 자연스럽게 오른쪽 또는 하단으로 이어지게 구성한다.
- 자주 확인하거나 조정하는 미리보기, 저장, 확대/축소, 현재 선택 정보는 작업 화면 근처에 고정하거나 인접 배치한다.
- 재료 생성 화면과 재료 조합 화면을 한 덩어리로 보이게 만들지 않는다.
- 사용자가 만든 재료를 조합하거나 매핑하는 화면에서는 "보유한 재료", "현재 사용 중인 재료", "저장될 결과"가 구분되어야 한다.
- 화면에 영어 키가 필요하더라도 사용자가 보는 선택지와 설명은 한국어 라벨을 우선 제공한다.

## GhostNest Final Report Override

- 최종 응답은 짧더라도 `완료한 범위`, `검증`, `남은 단계`, `다음 우선순위`, `남은 리스크`를 생략하지 않는다.
- 남은 단계가 있으면 "완료"만 말하지 않고 "부분 완료" 상태로 보고한다.
- 남은 단계가 없을 때만 "남은 단계 없음"이라고 적는다.
- UI 검증이 정적 검증에 그쳤다면 반드시 "UX 검증 미완료"라고 적는다.

## GhostNest Commit / Push Routine

- 사용자가 커밋/푸시를 요청하면 먼저 현재 작업의 완료 범위와 남은 계획을 `docs/local-notes/`의 기존 개발 계획 형식으로 기록한다.
- 기록에는 `Completed`, `Verification`, `Remaining Work`, `Next Priority`, `Risks`에 해당하는 내용이 들어가야 한다.
- 기록 후에만 stage, commit, push를 진행한다.
- `docs/local-notes/`, `.agents/`, `.codex/`, 임시 UI 검증 파일은 커밋 대상에 포함하지 않는다.
